---
title: "No Ianitor In Function Body"
description: "Disallow Ianitor validator creation inside function bodies. Hoist to module scope to avoid recreating validators on every call."
---

import Badge from "@/components/badge.astro";
import RuleExample from "@/components/rule-example.astro";
import { Aside, CardGrid, LinkCard } from "@astrojs/starlight/components";

<Badge variant="error" />
<Badge variant="roblox" />

`small-rules/no-ianitor-in-function-body` reports code described by its source metadata: `disallow Ianitor validator creation inside function bodies. Hoist to module scope to avoid recreating validators on every call`. It reports with the message `Ianitor validator created inside function body is slow — hoist to module scope. Example: const validator = Ianitor.keyOf(ids);`.

## Rule Details

This rule is configured as `small-rules/no-ianitor-in-function-body` and reports the concrete syntax covered by its implementation in `src/rules/no-ianitor-in-function-body.ts`.

<Aside type="note">

Rule message:

```text
hoistIanitorValidator: Ianitor validator created inside function body is slow — hoist to module scope. Example: const validator = Ianitor.keyOf(ids);
```

</Aside>

## Configuration

```json
{
	"plugins": ["@pobammer-ts/small-rules"],
	"rules": {
		"small-rules/no-ianitor-in-function-body": "error"
	}
}
```

## Examples

<RuleExample type="fail" title="Fails">

```ts
import { Ianitor } from "@packages/ianitor";

const placementVfxIds = { a: "a", b: "b" } as const;

export function isPlacementVfxId(value: unknown) {
	return Ianitor.keyOf(placementVfxIds)(value).success;
}
```

</RuleExample>

<RuleExample type="pass" title="Passes">

```ts
import { Ianitor } from "@packages/ianitor";

const placementVfxIds = { a: "a", b: "b" } as const;
const isPlacementVfxIdIanitor = Ianitor.keyOf(placementVfxIds);

export function isPlacementVfxId(value: unknown) {
	return isPlacementVfxIdIanitor(value).success;
}
```

</RuleExample>

## Why?

<CardGrid>
	<LinkCard
		title="Respect Roblox semantics"
		description="Roblox and Luau APIs have runtime costs and conventions that generic JavaScript rules do not catch."
		href="/small-rules/rules/roblox/no-ianitor-in-function-body/"
	/>
	<LinkCard
		title="Fail risky patterns early"
		description="Problem rules turn subtle mistakes into clear lint failures before they reach runtime."
		href="/small-rules/configuration/"
	/>
	<LinkCard
		title="Review the intent"
		description="Manual fixes keep the developer in control when the best rewrite depends on context."
		href="/small-rules/configuration/"
	/>
</CardGrid>

## Related Rules

<CardGrid>
	<LinkCard title="Enforce Ianitor Check Type" href="/small-rules/rules/roblox/enforce-ianitor-check-type/" />
	<LinkCard title="No Ianitor Success Access" href="/small-rules/rules/roblox/no-ianitor-success-access/" />
	<LinkCard title="Ban Instances" href="/small-rules/rules/roblox/ban-instances/" />
	<LinkCard title="Memoized Effect Dependencies" href="/small-rules/rules/roblox/memoized-effect-dependencies/" />
</CardGrid>
