---
title: "No Events In Events Callback"
description: "Disallow sending Events back to the same player inside an Events.connect callback; use Functions for request/response."
---

import Badge from "@/components/badge.astro";
import RuleOptions from "@/components/rule-options.astro";
import RuleSummary from "@/components/rule-summary.astro";
import RuleExample from "@/components/rule-example.astro";
import { CardGrid, LinkCard } from "@astrojs/starlight/components";

<Badge variant="error" />
<Badge variant="roblox" />

<RuleSummary rule="no-events-in-events-callback" />

## Rule Details

This rule is configured as `small-rules/no-events-in-events-callback` and reports the concrete syntax covered by its implementation in `src/rules/no-events-in-events-callback.ts`.

## Configuration

<RuleOptions rule="no-events-in-events-callback" />

## Examples

<RuleExample type="fail" title="Fails">

```ts
import { Events } from "server/networking";

Events.units.unequipUnit.connect((player: Player, unitKey: string): void => {
	if (unitKey.size() > 0) {
		Events.promptNotification.fire(player, "error");
	}
});
```

</RuleExample>

<RuleExample type="pass" title="Passes">

```ts
import { Events } from "server/networking";

Events.units.unequipUnit.connect((player: Player, otherPlayer: Player): void => {
	Events.promptNotification.fire(otherPlayer, "error");
});
```

</RuleExample>

## Why?

<CardGrid>
	<LinkCard
		title="Respect Roblox semantics"
		description="Roblox and Luau APIs have runtime costs and conventions that generic JavaScript rules do not catch."
		href="/small-rules/rules/roblox/no-events-in-events-callback/"
	/>
	<LinkCard
		title="Fail risky patterns early"
		description="Problem rules turn subtle mistakes into clear lint failures before they reach runtime."
		href="/small-rules/configuration/"
	/>
	<LinkCard
		title="Review the intent"
		description="Manual fixes keep the developer in control when the best rewrite depends on context."
		href="/small-rules/configuration/"
	/>
</CardGrid>

## Related Rules

<CardGrid>
	<LinkCard title="Ban Instances" href="/small-rules/rules/roblox/ban-instances/" />
	<LinkCard title="Enforce Ianitor Check Type" href="/small-rules/rules/roblox/enforce-ianitor-check-type/" />
	<LinkCard title="Memoized Effect Dependencies" href="/small-rules/rules/react/memoized-effect-dependencies/" />
</CardGrid>
