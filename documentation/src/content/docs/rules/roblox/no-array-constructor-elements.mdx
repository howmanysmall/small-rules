---
title: "No Array Constructor Elements"
description: "Disallow array constructor element forms and enforce roblox-ts-aware constructor patterns."
---

import Badge from "@/components/badge.astro";
import RuleExample from "@/components/rule-example.astro";
import { Aside, CardGrid, LinkCard } from "@astrojs/starlight/components";

<Badge variant="error" />
<Badge variant="fixable" />
<Badge variant="roblox" />

`small-rules/no-array-constructor-elements` reports code described by its source metadata: `disallow array constructor element forms and enforce roblox-ts-aware constructor patterns`. It reports with the message `Do not use Array constructor enumeration arguments. Use an array literal instead.`.

## Rule Details

This rule is configured as `small-rules/no-array-constructor-elements` and reports the concrete syntax covered by its implementation in `src/rules/no-array-constructor-elements.ts`.

<Aside type="note">

Rule messages:

```text
avoidConstructorEnumeration: Do not use Array constructor enumeration arguments. Use an array literal instead.
avoidLengthConstructorInStandard: Length-based Array constructor is not allowed in standard mode. Prefer Array.from({ length: n }).
avoidSingleArgumentConstructor: Single-argument Array constructor form is not allowed here. Use an array literal instead.
collapseArrayPushInitialization: Collapse new Array<T>() + consecutive .push(...) calls into a single array literal initializer.
requireExplicitGenericOnNewArray: new Array() must use an explicit generic argument or a contextual Array<T>/ReadonlyArray<T> annotation.
suggestArrayFromLength: Replace with Array.from({ length: value }).
suggestArrayLiteral: Replace constructor form with an array literal.
suggestCollapseArrayPushInitialization: Collapse constructor + push sequence into a single array literal initializer.
```

</Aside>

## Configuration

```json
{
	"plugins": ["@pobammer-ts/small-rules"],
	"rules": {
		"small-rules/no-array-constructor-elements": [
			"error",
			{
				"environment": "roblox-ts",
				"requireExplicitGenericOnNewArray": true
			}
		]
	}
}
```

## Examples

<RuleExample type="fail" title="Fails">

```ts
const values = new Array("a", "b");
```

</RuleExample>

When auto-fixed, this becomes:

```ts
const values = ["a", "b"];
```

<RuleExample type="pass" title="Passes">

```ts
const value = new Set();
```

</RuleExample>

## Why?

<CardGrid>
	<LinkCard
		title="Respect Roblox semantics"
		description="Roblox and Luau APIs have runtime costs and conventions that generic JavaScript rules do not catch."
		href="/small-rules/rules/roblox/no-array-constructor-elements/"
	/>
	<LinkCard
		title="Fail risky patterns early"
		description="Problem rules turn subtle mistakes into clear lint failures before they reach runtime."
		href="/small-rules/configuration/"
	/>
	<LinkCard
		title="Automate the safe edit"
		description="The rule includes a fixer so repeated cleanup can be delegated to Oxlint."
		href="/small-rules/configuration/"
	/>
</CardGrid>

## Related Rules

<CardGrid>
	<LinkCard title="No Array Size Assignment" href="/small-rules/rules/roblox/no-array-size-assignment/" />
	<LinkCard title="No Color3 Constructor" href="/small-rules/rules/roblox/no-color3-constructor/" />
	<LinkCard title="Ban Instances" href="/small-rules/rules/roblox/ban-instances/" />
	<LinkCard title="Enforce Ianitor Check Type" href="/small-rules/rules/roblox/enforce-ianitor-check-type/" />
</CardGrid>
