---
title: "No Unknown Returns"
---

import RulePage from "$components/rule-page.astro";

<RulePage rule="no-unknown-returns">
	<Fragment slot="rationale">
		<p>
			Ported from <a href="https://github.com/dmmulroy/anti-slop">dmmulroy/anti-slop</a>. A function that returns{" "}
			<code>unknown</code> pushes parsing work onto every caller. Decode once at the boundary and return the named
			domain contract instead.
		</p>
	</Fragment>
</RulePage>
