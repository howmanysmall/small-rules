---
title: "No Cascading Set State"
description: "Disallow effect hooks with many cascading state updates."
---

import Badge from "@/components/badge.astro";
import RuleExample from "@/components/rule-example.astro";
import { Aside, CardGrid, LinkCard } from "@astrojs/starlight/components";

<Badge variant="error" />

`small-rules/no-cascading-set-state` reports code described by its source metadata: `disallow effect hooks with many cascading state updates`. It reports with the message `{{count}} setState calls in a single useEffect — consider using useReducer or deriving state`.

## Rule Details

This rule is configured as `small-rules/no-cascading-set-state` and reports the concrete syntax covered by its implementation in `src/rules/no-cascading-set-state.ts`.

<Aside type="note">

Rule message:

```text
cascadingSetState: {{count}} setState calls in a single useEffect — consider using useReducer or deriving state
```

</Aside>

## Configuration

```json
{
	"plugins": ["@pobammer-ts/small-rules"],
	"rules": {
		"small-rules/no-cascading-set-state": "error"
	}
}
```

## Examples

<RuleExample type="fail" title="Fails">

```ts
import { useEffect, useState } from "@rbxts/react";

function Component() {
	const [firstValue, setFirstValue] = useState(0);
	const [secondValue, setSecondValue] = useState(0);
	const [thirdValue, setThirdValue] = useState(0);

	useEffect(() => {
		setFirstValue(firstValue + 1);
		setSecondValue(secondValue + 1);
		setThirdValue(thirdValue + 1);
	}, [firstValue, secondValue, thirdValue]);
}
```

</RuleExample>

<RuleExample type="pass" title="Passes">

```ts
import { useEffect, useState } from "@rbxts/react";

function Component() {
	const [firstValue, setFirstValue] = useState(0);
	const [secondValue, setSecondValue] = useState(0);

	useEffect(() => {
		setFirstValue(firstValue + 1);
		setSecondValue(secondValue + 1);
	}, [firstValue, secondValue]);
}
```

</RuleExample>

## Why?

<CardGrid>
	<LinkCard
		title="Protect render behavior"
		description="React components stay easier to profile when hooks, memoization, and JSX boundaries are explicit."
		href="/small-rules/rules/react/no-cascading-set-state/"
	/>
	<LinkCard
		title="Fail risky patterns early"
		description="Problem rules turn subtle mistakes into clear lint failures before they reach runtime."
		href="/small-rules/configuration/"
	/>
	<LinkCard
		title="Review the intent"
		description="Manual fixes keep the developer in control when the best rewrite depends on context."
		href="/small-rules/configuration/"
	/>
</CardGrid>

## Related Rules

<CardGrid>
	<LinkCard title="Ban React FC" href="/small-rules/rules/react/ban-react-fc/" />
	<LinkCard title="No Giant Component" href="/small-rules/rules/react/no-giant-component/" />
	<LinkCard title="No God Components" href="/small-rules/rules/react/no-god-components/" />
	<LinkCard
		title="No Inline Property On Memo Component"
		href="/small-rules/rules/react/no-inline-property-on-memo-component/"
	/>
</CardGrid>
