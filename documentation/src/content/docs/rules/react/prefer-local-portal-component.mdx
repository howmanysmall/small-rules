---
title: "Prefer Local Portal Component"
description: "Prefer a local Portal component over direct createPortal calls when the project already defines one."
---

import Badge from "@/components/badge.astro";
import RuleOptions from "@/components/rule-options.astro";
import RuleSummary from "@/components/rule-summary.astro";
import RuleExample from "@/components/rule-example.astro";
import { CardGrid, LinkCard } from "@astrojs/starlight/components";

<Badge variant="suggestion" />
<Badge variant="fixable" />

<RuleSummary rule="prefer-local-portal-component" />

## Rule Details

This rule is configured as `small-rules/prefer-local-portal-component` and reports the concrete syntax covered by its implementation in `src/rules/prefer-local-portal-component.ts`.

## Configuration

<RuleOptions rule="prefer-local-portal-component" />

## Examples

The failing example assumes the project has a resolvable local `Portal` component.

<RuleExample type="fail" title="Fails">

```tsx
import Portal from "../components/portal";
import { createPortal } from "@rbxts/react-roblox";

export function Example(target: Instance) {
	return createPortal(<frame />, target);
}
```

</RuleExample>

When auto-fixed, this becomes:

```tsx
import Portal from "../components/portal";
import { createPortal } from "@rbxts/react-roblox";

export function Example(target: Instance) {
	return (
		<Portal target={target}>
			<frame />
		</Portal>
	);
}
```

<RuleExample type="pass" title="Passes">

```tsx
import Portal from "../components/portal";

export function Example(target: Instance) {
	return (
		<Portal target={target}>
			<frame />
		</Portal>
	);
}
```

</RuleExample>

## Why?

<CardGrid>
	<LinkCard
		title="Protect render behavior"
		description="React components stay easier to profile when hooks, memoization, and JSX boundaries are explicit."
		href="/small-rules/rules/react/prefer-local-portal-component/"
	/>
	<LinkCard
		title="Guide consistent style"
		description="Suggestions document a preferred shape without treating every occurrence as a correctness bug."
		href="/small-rules/configuration/"
	/>
	<LinkCard
		title="Automate the safe edit"
		description="The rule includes a fixer so repeated cleanup can be delegated to Oxlint."
		href="/small-rules/configuration/"
	/>
</CardGrid>

## Related Rules

<CardGrid>
	<LinkCard title="No Giant Component" href="/small-rules/rules/react/no-giant-component/" />
	<LinkCard
		title="No Inline Property On Memo Component"
		href="/small-rules/rules/react/no-inline-property-on-memo-component/"
	/>
	<LinkCard title="Require React Component Keys" href="/small-rules/rules/react/require-react-component-keys/" />
	<LinkCard title="Strict Component Boundaries" href="/small-rules/rules/react/strict-component-boundaries/" />
</CardGrid>
