---
title: "Prefer Use Reducer"
description: "Suggest using useReducer for related state updates instead of multiple useState calls."
---

import Badge from "@/components/badge.astro";
import RuleOptions from "@/components/rule-options.astro";
import RuleSummary from "@/components/rule-summary.astro";
import RuleExample from "@/components/rule-example.astro";
import { CardGrid, LinkCard } from "@astrojs/starlight/components";

<Badge variant="error" />

<RuleSummary rule="prefer-use-reducer" />

## Rule Details

This rule is configured as `small-rules/prefer-use-reducer` and reports the concrete syntax covered by its implementation in `src/rules/prefer-use-reducer.ts`.

## Configuration

<RuleOptions rule="prefer-use-reducer" />

## Examples

<RuleExample type="fail" title="Fails">

```ts
import { useState } from "@rbxts/react";

function Component() {
	const [firstValue] = useState(0);
	const [secondValue] = useState(0);
	const [thirdValue] = useState(0);
	const [fourthValue] = useState(0);
	const [fifthValue] = useState(0);
}
```

</RuleExample>

<RuleExample type="pass" title="Passes">

```ts
import { useState } from "@rbxts/react";

function Component() {
	const [firstValue] = useState(0);
	const [secondValue] = useState(0);
	const [thirdValue] = useState(0);
	const [fourthValue] = useState(0);
}
```

</RuleExample>

## Why?

<CardGrid>
	<LinkCard
		title="Protect render behavior"
		description="React components stay easier to profile when hooks, memoization, and JSX boundaries are explicit."
		href="/small-rules/rules/react/prefer-use-reducer/"
	/>
	<LinkCard
		title="Fail risky patterns early"
		description="Problem rules turn subtle mistakes into clear lint failures before they reach runtime."
		href="/small-rules/configuration/"
	/>
	<LinkCard
		title="Review the intent"
		description="Manual fixes keep the developer in control when the best rewrite depends on context."
		href="/small-rules/configuration/"
	/>
</CardGrid>

## Related Rules

<CardGrid>
	<LinkCard title="Ban React FC" href="/small-rules/rules/react/ban-react-fc/" />
	<LinkCard title="No Cascading Set State" href="/small-rules/rules/react/no-cascading-set-state/" />
	<LinkCard title="No Giant Component" href="/small-rules/rules/react/no-giant-component/" />
	<LinkCard title="No God Components" href="/small-rules/rules/react/no-god-components/" />
</CardGrid>
