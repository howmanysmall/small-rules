---
title: "Prefer Ternary Conditional Rendering"
description: "Prefer ternary expressions over complementary JSX && branches."
---

import Badge from "@/components/badge.astro";
import RuleOptions from "@/components/rule-options.astro";
import RuleSummary from "@/components/rule-summary.astro";
import RuleExample from "@/components/rule-example.astro";
import { CardGrid, LinkCard } from "@astrojs/starlight/components";

<Badge variant="suggestion" />
<Badge variant="fixable" />

<RuleSummary rule="prefer-ternary-conditional-rendering" />

## Rule Details

This rule is configured as `small-rules/prefer-ternary-conditional-rendering` and reports the concrete syntax covered by its implementation in `src/rules/prefer-ternary-conditional-rendering.ts`.

## Configuration

<RuleOptions rule="prefer-ternary-conditional-rendering" />

## Examples

<RuleExample type="fail" title="Fails">

```tsx
function Component({ gradient, gradientToUse, rarityStyle }) {
	return (
		<>
			{gradient !== undefined && <uigradient key="ui-gradient" Color={gradient} />}
			{gradient === undefined && (
				<AnimatedGradient
					key="animated-gradient"
					colorValue={gradientToUse}
					rotation={45}
					sweepingSpeed={rarityStyle?.sweepingSpeed ?? 0}
				/>
			)}
		</>
	);
}
```

</RuleExample>

When auto-fixed, this becomes:

```tsx
function Component({ gradient, gradientToUse, rarityStyle }) {
	return (
		<>
			{gradient !== undefined ? (
				<uigradient key="ui-gradient" Color={gradient} />
			) : (
				<AnimatedGradient
					key="animated-gradient"
					colorValue={gradientToUse}
					rotation={45}
					sweepingSpeed={rarityStyle?.sweepingSpeed ?? 0}
				/>
			)}
		</>
	);
}
```

<RuleExample type="pass" title="Passes">

```tsx
function Component({ flag }) {
	return <>{flag ? <A /> : <B />}</>;
}
```

</RuleExample>

## Why?

<CardGrid>
	<LinkCard
		title="Protect render behavior"
		description="React components stay easier to profile when hooks, memoization, and JSX boundaries are explicit."
		href="/small-rules/rules/react/prefer-ternary-conditional-rendering/"
	/>
	<LinkCard
		title="Guide consistent style"
		description="Suggestions document a preferred shape without treating every occurrence as a correctness bug."
		href="/small-rules/configuration/"
	/>
	<LinkCard
		title="Automate the safe edit"
		description="The rule includes a fixer so repeated cleanup can be delegated to Oxlint."
		href="/small-rules/configuration/"
	/>
</CardGrid>

## Related Rules

<CardGrid>
	<LinkCard title="Ban React FC" href="/small-rules/rules/react/ban-react-fc/" />
	<LinkCard title="No Cascading Set State" href="/small-rules/rules/react/no-cascading-set-state/" />
	<LinkCard title="No Giant Component" href="/small-rules/rules/react/no-giant-component/" />
	<LinkCard title="No God Components" href="/small-rules/rules/react/no-god-components/" />
</CardGrid>
