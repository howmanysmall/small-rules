---
title: "Introduction"
description: "What is small-rules and why should you use it?"
---

import { CardGrid, LinkCard } from "@astrojs/starlight/components";

import PageHeader from "@/components/PageHeader.astro";

<PageHeader
	title="Introduction"
	subtitle="What small-rules is, why it exists, and how it fits into the roblox-ts toolchain."
	kicker="Getting started"
/>

`@pobammer-ts/small-rules` is an oxlint-native lint plugin for roblox-ts projects. It provides 91 custom rules that catch common mistakes, enforce consistent patterns, and improve code quality across TypeScript, React Luau, and Roblox-specific APIs.

## Why oxlint?

Oxlint is a fast, Rust-based linter for JavaScript and TypeScript. It runs orders of magnitude faster than ESLint while covering the most common linting needs. `small-rules` extends oxlint with domain-specific rules that understand roblox-ts patterns, React component conventions, and Roblox API idioms.

## Rule Categories

The rules are organized into four categories:

<CardGrid>
	<LinkCard
		title="React Rules"
		href="/small-rules/rules/react/ban-react-fc/"
		description="Rules for React Luau components, hooks, and JSX patterns."
	/>
	<LinkCard
		title="Roblox & Luau Rules"
		href="/small-rules/rules/roblox/ban-instances/"
		description="Rules for Roblox instances, Ianitor, Color3, UDim2, and other Roblox APIs."
	/>
	<LinkCard
		title="Naming & Conventions"
		href="/small-rules/rules/naming/array-type-generic/"
		description="Rules for naming conventions, type style, and file naming."
	/>
	<LinkCard
		title="General Logic & Style"
		href="/small-rules/rules/general/prefer-early-return/"
		description="Rules for code quality, control flow, and common pitfalls."
	/>
</CardGrid>

## Design Principles

1. **Oxlint-native** — Rules use the oxlint rule API, not ESLint compatibility layers. This means faster execution and better integration with oxlint's diagnostic output.
2. ** roblox-ts aware** — Rules understand roblox-ts idioms like `Ianitor`, `Color3`, `UDim2`, `task.wait()`, and React Luau components.
3. **Minimal configuration** — Most rules work with zero configuration. Options are available for rules that need them, but sensible defaults cover the common cases.
4. **Auto-fixable where safe** — Many rules provide automatic fixes that can be applied with `oxlint --fix`.

## Compatibility

`small-rules` requires:

- `oxlint` >= 1.0.0
- TypeScript >= 5.0 (for roblox-ts projects)
- An oxlint configuration file (`oxlint.json` or `.oxlintrc.json`)
