---
title: "small-rules"
description: "Oxlint-native rules for TypeScript, React, and roblox-ts"
tableOfContents: false
---

import HeroSplash from "@/components/HeroSplash.astro";
import CategoryCard from "@/components/CategoryCard.astro";
import FeatureCard from "@/components/FeatureCard.astro";
import Icon from "@/components/Icon.astro";
import { CardGrid, LinkCard } from "@astrojs/starlight/components";
import { homeFacts } from "@/data/home-facts";

<HeroSplash
	kicker="Oxlint-native linting for roblox-ts"
	title="small-rules"
	subtitle={`${homeFacts.totalRules} source-backed rules for React Luau, Ianitor lifecycle, Roblox UI conventions, and TypeScript quality.`}
/>

<div class="home-proof reveal">
	<p class="home-proof-copy">
		Built for teams that want native oxlint speed without giving up roblox-ts-specific checks.
	</p>
	<div class="home-proof-grid">
		<div class="home-proof-item">
			<div class="home-proof-value">{homeFacts.totalRules}</div>
			<div class="home-proof-label">Current rules</div>
			<div class="home-proof-hint">verified from the plugin source</div>
		</div>
		<div class="home-proof-item">
			<div class="home-proof-value">{homeFacts.fixableRules}</div>
			<div class="home-proof-label">Auto-fixable</div>
			<div class="home-proof-hint">rules that currently advertise code fixes</div>
		</div>
		<div class="home-proof-item">
			<div class="home-proof-value">{homeFacts.totalCategories}</div>
			<div class="home-proof-label">Categories</div>
			<div class="home-proof-hint">react, roblox, naming, general</div>
		</div>
		<div class="home-proof-item">
			<div class="home-proof-value">Oxlint</div>
			<div class="home-proof-label">Native plugin</div>
			<div class="home-proof-hint">no ESLint compatibility layer</div>
		</div>
	</div>
</div>

<div class="section-header reveal">
	<span class="section-eyebrow">
		<Icon name="rocket" size={12} /> Start here
	</span>
	<h2>Use the docs like a reference, not a landing page</h2>
	<p>Start with installation, copy a minimal config, then browse rules by category as you tighten the codebase.</p>
</div>

<CardGrid class="home-start-grid reveal">
	<LinkCard
		title="Quick Start"
		href="/small-rules/quick-start/"
		description="Install the plugin, register it in oxlint, and run the first pass."
	/>
	<LinkCard
		title="Configuration"
		href="/small-rules/configuration/"
		description="See plugin registration, rule severity setup, and practical defaults."
	/>
	<LinkCard
		title="Changelog"
		href="/small-rules/changelog/"
		description="Track new rules, renamed behavior, and release-level changes."
	/>
</CardGrid>

<div class="section-header reveal">
	<span class="section-eyebrow">
		<Icon name="search" size={12} /> Browse
	</span>
	<h2>Pick the part of the codebase you want to harden</h2>
	<p>These category counts reflect the current plugin, not stale marketing numbers.</p>
</div>

<div class="category-grid reveal">
	<CategoryCard
		slug="ban-react-fc"
		label="React & React Luau"
		count={homeFacts.categories.react}
		description="Component patterns, hooks, memoization, and React Luau-specific checks."
		category="react"
	/>
	<CategoryCard
		slug="ban-instances"
		label="Roblox & Luau"
		count={homeFacts.categories.roblox}
		description="Instances, Ianitor lifecycle, UI constraints, modding helpers, and unsafe globals."
		category="roblox"
	/>
	<CategoryCard
		slug="array-type-generic"
		label="Naming & Conventions"
		count={homeFacts.categories.naming}
		description="Naming rules, enum style, async suffixes, and abbreviation cleanup."
		category="naming"
	/>
	<CategoryCard
		slug="prefer-early-return"
		label="General Logic & Style"
		count={homeFacts.categories.general}
		description="Control flow, imports, directives, and TypeScript hygiene that cuts across the repo."
		category="general"
	/>
</div>

<div class="section-header reveal">
	<span class="section-eyebrow">
		<Icon name="sparkles" size={12} /> Why it exists
	</span>
	<h2>Focused rules for the failures oxlint does not catch by itself</h2>
	<p>The plugin is strongest where roblox-ts teams hit ecosystem-specific footguns and repeated review churn.</p>
</div>

<div class="feature-grid reveal">
	<FeatureCard
		icon="bolt"
		title="Oxlint-native"
		description="Ships as a real oxlint plugin, so you keep the native execution model instead of routing through ESLint."
	/>
	<FeatureCard
		icon="puzzle"
		title="Roblox-aware"
		description="Targets React Luau patterns, Ianitor lifecycle rules, UI conventions, and Roblox-specific APIs that generic linters miss."
	/>
	<FeatureCard
		icon="wand"
		title="Fixes where they help"
		description={`${homeFacts.fixableRules} rules currently expose code fixes, so you can use oxlint --fix for the repetitive cleanup.`}
	/>
</div>
